# Mosquitto — demo broker (slice S0)
# Anonymous listener for the simulated MQTT feed (ผนวก ๑๓ topic ๑).
listener 1883
allow_anonymous true

# WebSocket listener (optional browser diagnostics)
listener 9001
protocol websockets

persistence false
log_dest stdout
log_type warning
