# PWA demo API — pinned (Codex #12). Python 3.13.
fastapi==0.115.6
uvicorn[standard]==0.34.0
paho-mqtt==2.1.0
psycopg[binary,pool]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0

# Model serving (slice S6). The API now loads and scores the artifact in-process, so it
# needs the same runtime the model was fitted with. Pins MUST match ml/requirements.txt:
# unpickling a scikit-learn estimator across versions is not supported, and a mismatch
# fails at load time on the judge's machine rather than in CI.
scikit-learn==1.6.0
numpy==2.2.1
joblib==1.4.2

# dev / gates
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.6
mypy==1.14.1
